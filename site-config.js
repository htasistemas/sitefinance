window.SITE_CONFIG = {
  version: '1.04',
  buildTimestamp: '2025-01-29T00:00:00.000Z',
};

// Backend base URL (ajuste para o ambiente de produção)
window.BACKEND_BASE_URL = window.BACKEND_BASE_URL || 'http://localhost:3001';
